# dsh bundle layer: mount the CLIProxyAPI adapter.
#
# Registration does not contact the proxy. Catalog requests discover available
# models; dispatch resolves credentials again for each call.
- insert:
    - id: dsh-cliproxy
      name: 'dsh-cliproxy'
